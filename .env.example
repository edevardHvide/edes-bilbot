# Finn Car Scraper Environment Variables

# Default search settings
DEFAULT_SEARCH_URL=https://www.finn.no/mobility/search/car?model=1.775.2000447&price_from=190000&price_to=280000&registration_class=1
DEFAULT_LIMIT=5
DEFAULT_ATTRIBUTES=Modellår,Kilometerstand,1. gang registrert,Totalpris,Girkasse,Drivstoff

# Scraping configuration
# Set to 'True' to enable detailed debug logging
DEBUG_MODE=False
LOG_FILE=scraper_debug.log
# Set to 'True' to save logs to file (otherwise logs only to console)
ENABLE_FILE_LOGGING=False

# Webdriver configuration
HEADLESS_MODE=True
CHROME_ARGS=--disable-gpu,--no-sandbox,--disable-dev-shm-usage,--disable-extensions

# Rate limiting (to avoid being blocked)
# Pause between requests in seconds
REQUEST_DELAY=3 